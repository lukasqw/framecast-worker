name: 'CI — Go Test'
description: 'Unit tests (mocks/fakes, sem infra externa) + coverage gate + vulnerability check'

inputs:
  go_version:
    description: 'Go version'
    required: false
    default: '1.25'
  coverage_gate:
    description: 'Minimum coverage % (must be ≥ this value)'
    required: false
    default: '85'

runs:
  using: composite
  steps:
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ inputs.go_version }}
        cache: true

    - name: Run tests
      shell: bash
      # cmd/ é só wiring de bootstrap — excluído do cálculo de cobertura.
      run: go test $(go list ./... | grep -v /cmd/) -race -coverprofile=cover.out -covermode=atomic

    - name: Coverage gate
      shell: bash
      run: |
        COVERAGE=$(go tool cover -func=cover.out | grep total | awk '{print $3}' | tr -d '%')
        echo "Cobertura: ${COVERAGE}%"
        if (( $(echo "$COVERAGE < ${{ inputs.coverage_gate }}" | bc -l) )); then
          echo "::error::Cobertura abaixo do gate (${{ inputs.coverage_gate }}%): ${COVERAGE}%"
          exit 1
        fi

    - name: Vulnerability check
      shell: bash
      run: |
        go install golang.org/x/vuln/cmd/govulncheck@latest
        govulncheck ./...
